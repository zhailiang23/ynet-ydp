# frontend-practice 生产环境配置示例
#
# ⚠️ 重要说明:
# 此文件仅供参考，实际部署不再需要此文件！
#
# 从 2024-12 开始，前端使用多环境配置文件 (.env.fat, .env.uat, .env.pro)
# 在 Docker 构建时通过 --build-arg BUILD_ENV=xxx 参数注入环境变量
# 这些变量会在构建时打包到镜像中，运行时无需额外配置
#
# 如果需要修改 API 地址，请编辑对应的环境配置文件：
# - FAT 环境: .env.fat (192.168.153.111)
# - UAT 环境: .env.uat (待配置)
# - 生产环境: .env.pro (待配置)

# Node.js 环境
NODE_ENV=production

# 后端 API 配置
# 注意: Next.js 的 NEXT_PUBLIC_ 前缀的变量会在构建时被注入到前端代码中
# 修改 API 地址请编辑 .env.{environment} 文件并重新构建镜像
NEXT_PUBLIC_API_BASE_URL=http://192.168.153.111:48080/admin-api
NEXT_PUBLIC_TENANT_ID=1

# 时区配置
TZ=Asia/Shanghai

# 日志级别（可选）
# LOG_LEVEL=info

# 其他自定义配置（根据需要添加）
# NEXT_PUBLIC_CUSTOM_VAR=value
