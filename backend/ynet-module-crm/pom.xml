<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>com.ynet.iplatform</groupId>
        <artifactId>iplatform</artifactId>
        <version>${revision}</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>
    <artifactId>ynet-module-crm</artifactId>
    <packaging>jar</packaging>

    <name>${project.artifactId}</name>
    <description>
        crm 包下，客户关系管理（Customer Relationship Management）。
        例如说：客户、联系人、商机、合同、回款等等
    </description>

    <dependencies>
        <dependency>
            <groupId>com.ynet.iplatform</groupId>
            <artifactId>iplatform-module-system</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>com.ynet.iplatform</groupId>
            <artifactId>iplatform-module-infra</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>com.ynet.iplatform</groupId>
            <artifactId>iplatform-module-bpm</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>com.ynet.iplatform</groupId>
            <artifactId>iplatform-module-task</artifactId>
            <version>${revision}</version>
        </dependency>

        <!-- 业务组件 -->
        <dependency>
            <groupId>com.ynet.iplatform</groupId>
            <artifactId>iplatform-spring-boot-starter-biz-ip</artifactId>
        </dependency>
        <dependency>
            <groupId>com.ynet.iplatform</groupId>
            <artifactId>iplatform-spring-boot-starter-biz-tenant</artifactId>
        </dependency>

        <!-- Web 相关 -->
        <dependency>
            <groupId>com.ynet.iplatform</groupId>
            <artifactId>iplatform-spring-boot-starter-web</artifactId>
        </dependency>

        <dependency>
            <groupId>com.ynet.iplatform</groupId>
            <artifactId>iplatform-spring-boot-starter-security</artifactId>
        </dependency>

        <!-- DB 相关 -->
        <dependency>
            <groupId>com.ynet.iplatform</groupId>
            <artifactId>iplatform-spring-boot-starter-mybatis</artifactId>
        </dependency>

        <!-- 工具类相关 -->
        <dependency>
            <groupId>com.ynet.iplatform</groupId>
            <artifactId>iplatform-spring-boot-starter-excel</artifactId>
        </dependency>

        <!-- Test 测试相关 -->
        <dependency>
            <groupId>com.ynet.iplatform</groupId>
            <artifactId>iplatform-spring-boot-starter-test</artifactId>
        </dependency>
    </dependencies>
</project>
